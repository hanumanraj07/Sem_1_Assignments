#include <stdio.h>
int main ()
{
    /*
Write a program to input an integer n and check if it is a perfect number (sum of proper divisors equals n).
 Limit checking divisors up to sqrt(n) approximation using if conditions (assume n <= 1000, no loops, nested ifs).*/



    return 0;
}